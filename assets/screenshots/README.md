# Screenshots

Captured (reproducible — rerun the tapes in `assets/tapes/` with
[vhs](https://github.com/charmbracelet/vhs)):

- `dashboard.png` — startup dashboard
- `which-key.png` — leader-key discovery menu
- `../gifs/find-files.gif` — fuzzy find + project grep

Wanted (contributions welcome — see [CONTRIBUTING](../../CONTRIBUTING.md)):

Claude Code diff review · lazygit · markdown rendering · notebook cell
outputs · LaTeX + PDF side-by-side · debugger · tmux agent layout ·
diagnostics panel

Guidelines: JetBrainsMono Nerd Font + Tokyo Night (shipped defaults),
~1400×800. Prefer a committed `.tape` over a raw screen capture so it can
be regenerated after UI changes. Note: avoid recording real AI sessions
with authenticated accounts — mock or blur anything account-specific.
